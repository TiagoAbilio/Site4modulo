extends layout

block content

  h1 Excluir Gênero: #{genre.name}
  
  if genre_books.length
  
    p #[Apague os seguintes livros antes de tentar excluir este gênero.]
  
    div(style='margin-left:20px;margin-top:20px')

      h4 Books
    
      dl
      each book in genre_books
        dt 
          a(href=book.url) #{book.title}
        dd #{book.summary}

  else
    p Você realmente quer deletar esse gênero?
    
    form(method='POST' action='')
      div.form-group
        input#id.form-control(type='hidden',name='id', required='true', value=genre._id )

      button.btn.btn-primary(type='submit') Deletar